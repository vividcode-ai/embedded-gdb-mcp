# @vividcodeai/embedded-gdb-mcp-darwin-x64

适用于 macOS x86_64 的预编译二进制文件。这是 `@vividcodeai/embedded-gdb-mcp` 的平台依赖包。

**不要直接安装此包。** 请安装父包：

```bash
npm install @vividcodeai/embedded-gdb-mcp
```

---

[English Documentation](README.md)
